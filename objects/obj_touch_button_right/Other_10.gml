event_inherited();

// This block applies to obj_player.
// It runs the right function in it, causing the
// player to move right.
with (obj_player)
{
	// Sets right input flag to true
	right_input = true;
}